apiVersion: v1
kind: Namespace
metadata:
  name: teste-infra
---
# 1. A Aplicação de Teste (Agnhost - ideal para inspecionar cabeçalhos de rede)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-teste
  namespace: teste-infra
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app-teste
  template:
    metadata:
      labels:
        app: app-teste
    spec:
      containers:
      - name: echoserver
        image: registry.k8s.io/e2e-test-images/agnhost:2.43
        args:
        - netexec
        - --http-port=8080
        ports:
        - containerPort: 8080
---
# 2. O Serviço Interno (Dual-Stack)
apiVersion: v1
kind: Service
metadata:
  name: svc-teste
  namespace: teste-infra
spec:
  type: ClusterIP
  ipFamilyPolicy: PreferDualStack
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: app-teste
---
# 3. A Definição da Gateway 
# Como o Cilium está configurado com Host Network, este recurso instruirá o Cilium 
# a escutar e bindar na porta 80 de todas as interfaces físicas (IPv4 e IPv6) das máquinas físicas.
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-infra
  namespace: teste-infra
spec:
  gatewayClassName: cilium
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: Same
---
# 4. A Rota HTTP que responde pelo seu domínio acadêmico
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rota-teste
  namespace: teste-infra
spec:
  parentRefs:
  - name: gateway-infra
  hostnames:
  - "teste-infra.k8s.sj.ifsc.edu.br"
  rules:
  - backendRefs:
    - name: svc-teste
      port: 80
